

import java.io.InputStream;
import java.net.MalformedURLException;
import java.net.URL;



public class FalsePositive20 {

    protected static volatile Object instance = null;


	public static void main(String[] args) throws MalformedURLException {
		Object tmp = FalsePositive20.instance;
	
		String path = "/http://www.hhrthrrtrtr.com";
        if (!path.startsWith("/"))
            throw new MalformedURLException("Path '" + path +
                                            "' does not start with '/'");
        URL url = new URL( path.substring(1));
        try (InputStream is = url.openStream()) {
        } catch (Throwable t) {
        	//the catch block is unreachable
        	//because the try with resource wraps everything into
        	//a catch-Any block, so Throwable cannot be caught here.
            handleThrowable(t);
            url = null;
        }
//        return url;
	}

  
	private static void handleThrowable(Throwable t) {
		// TODO Auto-generated method stub
		System.err.println("HALLO!");
	}
		
	
}
